<div>
    <div>
        <%= link_to '戻る', root_path %>
        <% if isTeacher %>
            <%= link_to '科目を作成する', new_subject_path %>
        <% else %>
            <%= link_to '履修登録をする', courses_path %>
        <% end %>
    </div>
    <hr />
    <div>
        <% if isTeacher %>
            <%= render 'listForStaff' %>
            <%= render 'listAllSubjects' %>
        <% else %>
            <%= render 'listRegistrationCourses' %>
        <% end %>
    </div>
</div>